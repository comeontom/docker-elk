input {
	# tcp {
	# 	port => 5000
	# }
	beats {
		port => '5044'
	}
}
## Add your filters / logstash plugins configuration here
filter {
  date {
    match => ["time", "yyyy-MM-dd'T'HH:mm:ss.SSSSSSSSSZ"]
	target => ["@timestamp"]
  }
}


output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
}
